{
  "name": "ssho-fashion-test-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 8080",
    "start": "next start -p 8080",
    "build": "npm run clean && next build",
    "clean": "rmdir /s .next",
    "deploy": "npm install && npm run build && eb deploy ssho-fashion-test-web-real"
  },
  "dependencies": {
    "next": "10.0.8",
    "react": "17.0.1",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.34",
    "@types/react": "^17.0.3",
    "typescript": "^4.2.4"
  }
}
